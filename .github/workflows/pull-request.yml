name: Test Gen Doc

on:
  push:
    branches-ignore: [ 'gh-pages' ]

  workflow_dispatch:

jobs:
  createDocs:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3
      - name: Run script
        run: ./.github/scripts/doc-gen.sh
