name: Run Tests, Build Documentation

on:
  push:
    branches: [ main ]

jobs:
  buildAndTest:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
